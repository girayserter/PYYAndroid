<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.girayserter.pyyandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PYYAndroid">
        <activity android:name=".KullaniciDetayActivity"></activity>
        <activity android:name=".KullanicilarActivity" />
        <activity android:name=".PersonelGrubuOlusturActivity" />
        <activity android:name=".ProjeOlusturActivity" />
        <activity android:name=".KullaniciAnasayfaActivity" />
        <activity android:name=".AdminAnasayfaActivity" />
        <activity android:name=".FirstLoginSetPasswordActivity" />
        <activity android:name=".KullaniciGirisActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>